<!DOCTYPE html>
<html>

<head>
  <title>Green Policy | Student Dashboard</title>

  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Professional Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <!-- Premium UI CSS -->
  <link rel="stylesheet" href="/css/premium.css">

  <!-- Theme Toggle Script -->
  <script src="/js/theme.js"></script>

</head>

<body>

  <nav class="navbar px-4 py-2">
    <span class="navbar-brand mb-0 h1 d-flex align-items-center gap-2">
      <i class="bi bi-shield-lock"></i> Green Policy
      <span class="text-muted fs-6 ms-2 pb-1 border-start border-secondary ps-3">Student Portal</span>
    </span>

    <div class="d-flex align-items-center gap-3">
      <button class="btn btn-outline-light btn-sm rounded-circle d-flex align-items-center justify-content-center"
        style="width: 32px; height: 32px;" onclick="toggleFormalTheme()" title="Toggle Dark/Light Mode">
        <i id="theme-icon" class="bi bi-moon-stars"></i>
      </button>

      <a href="/logout" class="btn btn-outline-danger btn-sm px-3">
        <i class="bi bi-box-arrow-right"></i> Logout
      </a>
    </div>
  </nav>

  <div class="container py-4">

    <div class="d-flex justify-content-between align-items-center mb-4 pb-2 border-bottom">
      <h3 class="mb-0 text-primary fw-semibold">My Assigned Policies</h3>
      <div class="text-muted small">
        <i class="bi bi-mortarboard"></i> Student Access
      </div>
    </div>


    <div class="row g-4">

      <% if(policies.length===0){ %>

        <div class="col-12 text-center py-5">
          <div class="text-muted">
            <i class="bi bi-inbox fs-1 d-block mb-3"></i>
            <p class="mb-0">No policy assigned at this moment.</p>
          </div>
        </div>

        <% } %>


          <% policies.forEach(p=> { %>

            <div class="col-md-6 col-lg-4">

              <div class="card glass-card h-100">

                <div class="card-body d-flex flex-column">

                  <h5 class="card-title fw-semibold mb-3 border-bottom pb-2">
                    <%= p.title %>
                  </h5>

                  <p class="text-muted small mb-4 flex-grow-1">
                    <%= p.description %>
                  </p>

                  <div class="p-3 rounded mb-3 border" style="background: var(--surface-color);">
                    <div class="d-flex justify-content-between mb-2">
                      <span class="text-muted small fw-medium">Department</span>
                      <span class="small fw-semibold">
                        <%= p.department %>
                      </span>
                    </div>
                    <div class="d-flex justify-content-between mb-2">
                      <span class="text-muted small fw-medium">Supervisor</span>
                      <span class="small fw-semibold">
                        <%= p.staff %>
                      </span>
                    </div>
                    <% if(p.deadline) { %>
                    <div class="d-flex justify-content-between">
                      <span class="text-danger small fw-medium">Deadline</span>
                      <span class="text-danger small fw-semibold">
                        <%= new Date(p.deadline).toLocaleDateString() %>
                      </span>
                    </div>
                    <% } %>
                  </div>

                  <div class="d-flex justify-content-between align-items-center pt-2 border-top">
                    <% if(p.status === 'completed') { %>
                      <span class="badge bg-success w-100 py-2"><i class="bi bi-check-circle me-1"></i> Policy Fulfilled</span>
                    <% } else { %>
                      <% if(!p.acknowledged) { %>
                        <form action="/student/acknowledge/<%= p.spid %>" method="POST" class="w-100">
                          <button type="submit" class="btn btn-primary btn-sm w-100"><i class="bi bi-check2-square me-1"></i> I Acknowledge</button>
                        </form>
                      <% } else { %>
                        <span class="badge bg-success w-100 py-2"><i class="bi bi-check-circle me-1"></i> Acknowledged</span>
                      <% } %>
                    <% } %>
                  </div>

                </div>

              </div>

            </div>

            <% }) %>

    </div>

  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>